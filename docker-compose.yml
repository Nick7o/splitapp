services:
  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: splitapp_user
      POSTGRES_PASSWORD: splitapp_dev_password
      POSTGRES_DB: splitapp_db
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
volumes:
  pgdata:
